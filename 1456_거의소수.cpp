#include <bits/stdc++.h>
using namespace std;

int main()
{
    float a, b;
    vector<int> a;
    P p;
    p.value()

    c >> a >> b;
    a = sqrt(a);
    b = sqrt(b);

    for (int i = 2; i < b; i++) {
        a.push(i);
    }

    // a <= 소수 < b 몇개인지
    for (int i = 0; i < b-2; i++) {
        for (int j = 0; i < ) {
            a[0]
        }
    }

    return 0;
}