#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;
    
    for (int i = 0; i < n; i++) {
        if (k < pow(10, i)) {

        }
    }

    return 0;
}